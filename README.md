# CBManager documentation
================
# Installation


Install cocoapods using
```
[sudo] gem install cocoapods
```
Install pods in project using
```
pod install
```

Close the project if already opened. Open project through 'xcworkspace' file
